# Оставляем пустым (или только комментарии).
# Так запросы из кода с (process.env.REACT_APP_API || '') пойдут
# относительными путями на тот же домен: /api/...
# Пример дополнительных опций (необязательно):
# GENERATE_SOURCEMAP=false
